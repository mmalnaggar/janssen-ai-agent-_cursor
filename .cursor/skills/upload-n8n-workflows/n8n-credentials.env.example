# n8n API credentials for workflow upload (copy to n8n-credentials.env and fill in)
# Do not commit n8n-credentials.env — it is gitignored.

N8N_BASE_URL="https://your-n8n-instance.com"
N8N_API_KEY="your_n8n_api_key_here"
