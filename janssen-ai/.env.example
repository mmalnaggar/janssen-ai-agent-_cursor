# Janssen AI - Environment Variables
# ============================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ============================================

# Server Configuration
PORT=3000
NODE_ENV=production

# Backend URL (public URL of this server)
BACKEND_URL=https://your-app.onrender.com

# n8n Configuration (optional - for n8n webhook integration)
N8N_WEBHOOK_URL=https://your-n8n.onrender.com/webhook

# Database Configuration (PostgreSQL)
# The app runs in fallback mode (no persistence) if PostgreSQL is unavailable
DB_HOST=localhost
DB_PORT=5432
DB_NAME=janssen_ai
DB_USER=postgres
DB_PASSWORD=your-password

# Render PostgreSQL connection string (overrides individual DB_ vars)
# DATABASE_URL=postgres://user:password@host:5432/janssen_ai

# OpenAI Configuration (optional - system works without it using keyword-based responses)
# OPENAI_API_KEY=sk-your-api-key
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_INTENT_MODEL=gpt-4o-mini
# OPENAI_MAX_TOKENS=500
# OPENAI_TEMPERATURE=0.7
# OPENAI_TIMEOUT_MS=15000

# WhatsApp Business API (optional)
# WHATSAPP_API_URL=https://graph.facebook.com/v17.0
# WHATSAPP_PHONE_NUMBER_ID=your-phone-id
# WHATSAPP_ACCESS_TOKEN=your-access-token
